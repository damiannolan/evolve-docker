# evolve-docker

Docker compose stack for running evolve stack with celestia underneath

## Usage

Launch docker compose stack.

```bash
make start
```

Teardown docker compose stack.

```bash
make stop
```
